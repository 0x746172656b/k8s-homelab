apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: sonarr-app
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/0x746172656b/homelab
    targetRevision: main
    path: sonarr
  destination:
    server: https://kubernetes.default.svc
    namespace: media
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
