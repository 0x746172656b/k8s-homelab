apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: sonarr
  namespace: flux-system
spec:
  interval: 1h
  path: ./sonarr
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
