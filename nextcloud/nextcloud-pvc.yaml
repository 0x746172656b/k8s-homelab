apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nextcloud-app-nextcloud
  namespace: nextcloud
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: synostorage
