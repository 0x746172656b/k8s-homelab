apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: monitoring-sync
  namespace: flux-system
spec:
  interval: 1h
  path: ./monitoring
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
