apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: khider.fr-cert
  namespace: web
spec:
  secretName: khider-fr-cert
  issuerRef:
    name: cloudflare-issuer
    kind: ClusterIssuer
  dnsNames:
    - khider.fr
    - www.khider.fr
